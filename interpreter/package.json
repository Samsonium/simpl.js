{
  "name": "interpreter",
  "version": "0.0.0",
  "description": "Simpl.js interpreter",
  "main": "index.js",
  "author": "Samsonium",
  "license": "MIT",
  "private": true,
  "scripts": {
    "build": "esbuild src/main.ts --outfile=./build/interpreter.js --minify --bundle --sourcemap --platform=node"
  },
  "devDependencies": {
    "esbuild": "latest"
  }
}
